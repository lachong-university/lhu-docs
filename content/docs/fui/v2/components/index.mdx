---
title: Danh mục component
description: 40 component dựng sẵn của FUI V2 — chọn cái nào, và cái nào cần import thêm
icon: Component
---

FUI V2 đăng ký sẵn **40 component** ở phạm vi global. Nguyên tắc chọn:

<Callout>
**Ưu tiên `f-*` trước, chỉ dùng `v-*` của Vuetify khi FUI không có sẵn.**

Component `f-*` đã tích hợp action, validate form, phím tắt, CRUD, tìm kiếm, xuất Excel — dùng
chúng thì viết ít JSON hơn nhiều.
</Callout>

| Ưu tiên dùng | Thay vì | Lý do |
|---|---|---|
| `f-table` | `v-data-table` | Sẵn sort, tìm kiếm, CRUD, xuất Excel. Bỏ `:update-api` là thành bảng chỉ đọc |
| `f-button` | `v-btn` | Hỗ trợ `CALL`, phím tắt, validate form |
| `f-date` / `f-time` | `v-text-field` + tự parse | Sẵn picker và mask khi gõ |
| `f-search` | `v-autocomplete` | Tìm qua API, có debounce và trạng thái loading |
| `f-menu` | `v-menu` + `v-btn` | Cấu hình gọn hơn, tích hợp icon và action |
| `f-echart` | `<canvas>`, thư viện khác | ECharts V5 — mọi loại biểu đồ |

## Nhập liệu

| Component | Dùng khi nào |
|---|---|
| [`f-button`](/docs/fui/v2/components/f-button) | Nút bấm có action, phím tắt, validate form |
| [`f-date`](/docs/fui/v2/components/f-date) | Nhập ngày, có picker và mask |
| [`f-time`](/docs/fui/v2/components/f-time) | Nhập giờ, có picker và mask |
| [`f-time-counter`](/docs/fui/v2/components/f-time-counter) | Đếm ngược hoặc đồng hồ thời gian thực |
| [`f-search`](/docs/fui/v2/components/f-search) | Autocomplete tìm qua API |
| [`f-menu`](/docs/fui/v2/components/f-menu) | Nút dropdown có icon, action, link |
| [`f-radiobox`](/docs/fui/v2/components/f-radiobox) | Nhóm radio dựng từ mảng |
| [`f-file-upload`](/docs/fui/v2/components/f-file-upload) | Tải file lên (Plupload, có dialog tiến trình) |
| [`f-image-update`](/docs/fui/v2/components/f-image-update) | Tải ảnh lên kèm cắt ảnh |
| [`f-excel-reader`](/docs/fui/v2/components/f-excel-reader) | Đọc file Excel/CSV thành JSON |
| [`f-qrcode`](/docs/fui/v2/components/f-qrcode) | Vẽ mã QR |
| [`f-qrcode-reader`](/docs/fui/v2/components/f-qrcode-reader) | Đọc QR bằng camera |

## Hiển thị

| Component | Dùng khi nào |
|---|---|
| [`f-label`](/docs/fui/v2/components/f-label) | Chip/badge hiển thị nhãn, trạng thái |
| [`f-box`](/docs/fui/v2/components/f-box) | Hiển thị và sửa dữ liệu dạng key–value |
| [`f-header`](/docs/fui/v2/components/f-header) | Tiêu đề lớn |
| [`f-title`](/docs/fui/v2/components/f-title) | Tiêu đề nhỏ |
| [`f-slider`](/docs/fui/v2/components/f-slider) | Carousel ảnh |
| [`f-pdfmake`](/docs/fui/v2/components/f-pdfmake) | Render PDF trong iframe |

## Bảng dữ liệu

| Component | Dùng khi nào |
|---|---|
| [`f-table`](/docs/fui/v2/components/f-table) | **Mọi bảng dữ liệu** — CRUD qua dialog hoặc chỉ đọc |
| [`f-sheet`](/docs/fui/v2/components/f-sheet) | Nhập liệu kiểu Excel — sửa thẳng trên ô, dán nhiều dòng |

Renderer dùng trong `headers` của `f-table`: tóm tắt cả 12 ở
[trang f-table](/docs/fui/v2/components/f-table#renderer-trong-ô), đi sâu 6 renderer có hành vi
riêng (`t-check`, `t-text`, `t-select`, `t-combobox`, `t-menu`, `t-link`) ở
[trang renderer trong ô](/docs/fui/v2/components/table-cell-renderers).

<Callout>
**`f-table` hay `f-sheet`?** `f-table` sửa từng dòng qua dialog form riêng. `f-sheet` sửa ngay
trên ô như Excel, dán được nhiều dòng từ bảng tính. Cả hai đều dựng sẵn — đừng tự viết bảng
nhập liệu bằng `uc-*.vue`.
</Callout>

## Dialog và soạn thảo

| Component | Dùng khi nào |
|---|---|
| [`f-dialog`](/docs/fui/v2/components/f-dialog) | Modal có form, watch, nút bấm — chỉ hợp form rất nhỏ |
| [`f-editor`](/docs/fui/v2/components/f-editor) | Soạn thảo rich text tại chỗ (CKEditor 5) |
| [`f-editor-dialog`](/docs/fui/v2/components/f-editor-dialog) | Soạn thảo rich text trong modal |

Mở một trang khác trong dialog/iframe (chi tiết một dòng, form phụ) thì dùng
[`openWindow()`](/docs/fui/v2/kien-truc/mo-cua-so) — **không** đặt `f-window` trực tiếp trong
`controls`, nó là component nội bộ do `openWindow` tự dựng.

<Callout type="warn">
**Không dùng `f-button` + `f-dialog` để mở form thêm mới** — không ổn định trong FUI V2. Dùng
`ctrl-update` của `f-table` cho CRUD inline. Cần dialog riêng thì dựng `v-dialog` thủ công.
</Callout>

## Biểu đồ

| Component | Dùng khi nào |
|---|---|
| [`f-echart`](/docs/fui/v2/components/f-echart) | Mọi loại biểu đồ — bar, line, pie, gauge, scatter, radar, sankey, tree, map |
| [`f-chart`](/docs/fui/v2/components/f-chart) | **Đừng dùng cho code mới** — bản Chart.js cũ, vẫn chạy nhưng không còn khuyến nghị |

## Component cần import thêm

Thiếu import thì component **không tồn tại** và vùng đó trắng trơn. Import không tự động —
phải đăng ký cho module bằng FUIX hoặc `file_import_new`.

| Component | Import bắt buộc |
|---|---|
| `f-echart` | `/include/chart/echarts.min.js` → `/include/chart/fechart.js` (**đúng thứ tự này**) |
| `f-sheet` | `/include/ag-grid/ag-grid.min.css` · `/include/ag-grid/ag-theme-alpine.min.css` · `/include/ag-grid/ag-grid-community.min.js` |
| `f-pdfmake` | `/include/pdfmake/pdfmake.min.js` · `print.min.js` · `vfs_fonts.js` |
| `f-file-upload` | `/include/plupload-2.3.7/plupload.full.min.js` |
| `f-image-update` | `/include/vue-advanced-cropper/index.umd.js` · `style.css` |
| `f-qrcode` | `/include/qrcode/qrcode.min.js` |
| `f-qrcode-reader` | `/include/qrcode/qrcode-reader.min.js` |
| `f-editor` · `f-editor-dialog` | `/include/ckeditor5/build/ckeditor.js` |

<Callout type="warn">
**Thứ tự nạp do `FileSort` quyết định, không phải phạm vi.** Server gộp import của cả framework,
project và module rồi sort chung một lần. `fechart.js` phụ thuộc biến global `echarts` nên bắt
buộc phải có `FileSort` **lớn hơn** `echarts.min.js` (ví dụ 100 và 110).
</Callout>

**Bundled sẵn, không cần import**: mọi `v-*` của Vuetify, `f-table`, `f-dialog`, `f-button`,
`f-date`, `f-time`, `f-search`, `f-menu`, `f-radiobox`, `f-box`, `f-label`, `f-header`,
`f-title`, `f-slider`, `f-excel-reader` — và mọi component không có tên trong bảng trên.

## Ngoài phạm vi tài liệu này

`defaultControlAttr` (bảng import mặc định trong `component.js`) còn khai 8 tag `f-*` cùng
`v-jsoneditor` **không có định nghĩa `Vue.component()`** trong `component.js` /
`componentTable.js` / `fechart.js` — component thật của chúng nằm ở file import riêng, không có
trong bộ script skill đọc được. Không đủ nguồn để viết props chính xác:

`f-account-manager` · `f-json-tree` · `f-code-editor` · `f-q-and-a` · `f-etest-review` ·
`f-audio` · `f-audio-recorder` · `f-google-login` · `v-jsoneditor`

Cần dùng một trong số này thì đọc trực tiếp file import tương ứng của project (qua
`file_import_get` nếu dùng FUI MCP), đừng đoán prop theo tên.

## Component nội bộ — đừng đặt trực tiếp trong `controls`

`header-bar` (khung menu trên cùng), `fp-profile` (khối hồ sơ người dùng, hiện qua
`set.userProfile`) và `f-window` (dialog do [`openWindow()`](/docs/fui/v2/kien-truc/mo-cua-so)
tự dựng) đều là component runtime tự quản lý theo cấu hình — không khai `{ "el": "..." }` trực
tiếp cho ba component này.
